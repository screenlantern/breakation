language: node_js
  - 6
  - 8
cache:
  directories:
    - "node_modules"
install:
  - npm install
services:
  - redis-server
  - mongodb
env:
  - TEST_DIR=client
  - TEST_DIR=server
script:
  - cd $TEST_DIR
  - npm build
  - npm test


