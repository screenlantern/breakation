language: node_js
  - 6
  - 8
cache:
  directories:
    - "node_modules"
install: true
services:
    - redis-server
    - mongodb
env:
  - TEST_DIR=client
  - TEST_DIR=server

script: cd $TEST_DIR && npm install && npm build && npm test


